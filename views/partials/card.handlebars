
  <div class="col-sm-3 mb-3 mb-sm-0">
    <div class="card">
      <div class="card-body">
        <a href="/product/{{nombre}}">
         <img src="/img/{{this.nombre}}.png" class="card-img-top" alt="{{nombre}}">
        <h5 class="card-title">{{nombre}}</h5></a>

        <!--<p class="card-text"><small class="text-body-secondary">{{author}}</small></p>
        <p class="card-text"><small class="text-body-secondary">{{date}}</small></p>-->
      </div>
    </div>
  </div>
